# Fluxion 0.63 candidate validation

## Published outcome

Full staging [34580758825](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34580758825)
passed on `cbbf71cbd2acbb74ca0b517ec031e8563b3bddb1`: all 801 tests, every
mandatory native gate, universal architecture/signature verification and real
DMG packaging. About made one actual anonymous GET with HTTP 200, remaining
quota 54, and no cookies, authorization, referrer or asset downloads. The retry
started after both earlier quota deadlines; no endpoint, authentication or
success fixture replaced the real check.

The inspected structure artifact confirms 30 operations, 24 exact hierarchy
keyboard-owner baselines, seven four-page/four-load checks, zero unaffected row
writes and one relocation for an adjacent swap. The unchanged staged DMG is
published as 0.63; see [provenance](../../release/provenance/v0.63.0-preview.1.md).
The following chronological notes preserve earlier failures and the evidence
available at each development step; their pending checks are resolved above.

## Earlier diagnostic and staging record

Third diagnostic [34573200035](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34573200035)
passed on `02646d694dfdebd22b3bc912f7702e7d63fcadbc`: all 801 tests and all 35
workflow steps succeeded. Its independently inspected structure report contains
30 operations, seven four-page live-state checks with exactly four server loads,
24 matching keyboard-owner baselines and zero unaffected row writes. The
following sections preserve the earlier candidate failures and corrections.
Full release staging remains separate; diagnostics did not check About or
produce a publishable DMG.

Full staging [34573639066](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34573639066)
tested the same source and passed all 801 tests, every other native gate, and
architecture/signature validation. About's single actual anonymous GET returned
HTTP 403 with exhausted quota (remaining 0), reset `1789111746`, or
`2026-09-11 07:29:06 UTC`. No cookies, authorization, referrer or asset requests
were sent. The UI displayed the retry time. Packaging, artifact promotion and
publication were correctly skipped; this run produced no distributable DMG.
No full staging retry may start before that reset, and a later retry must still
pass every gate and verify the exact resulting archive before publication.

Second full staging [34574614508](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34574614508)
ran on `60985e4fd4b84362f7c93f7ccf022ca20592a1f3` (only release-documentation
changes from the diagnostic source). It was dispatched at `07:29:23 UTC`, after
the previous reset. All 801 tests and every other native/architecture/signature
check passed again, but the actual anonymous About request returned HTTP 403,
remaining 0, with a new reset `1789112663` (`2026-09-11 07:44:23 UTC`). It sent
one GET, no cookies, authorization or referrer, and no asset requests. Packaging
and publication were skipped. This is the current retry deadline; no third full
staging run may start before it. The new run's independently inspected structure
report again passed all 30 operations and seven four-page/four-load checks.

This unpublished candidate adds whole-group drag reordering around ordinary
tabs, groups and splits, and intact split-pair drops into groups. Its keyboard
menu equivalent now moves around all top-level units, not just other groups.

The source uses current native-object membership and validates whole split
units before mutation. Foreign, closed, pinned and cross-workspace members are
rejected; web text cannot manufacture a privileged drag session. Drop-time
validation is separate from hover feedback, and cleanup runs on failed native
operations as well as successful drops.

Initial integrated checks passed 781 tests; the added runtime-handler and actor
registration tests brought that to 789. Two further tests check the live-state
verifier rejects document changes and reloads. The final combined suite passes
all 791 tests, with syntax checks and no failures or skips.
The expanded native structure gate requires 30 operations, with actual Flow
drag handlers and Gecko DataTransfer. It checks native placement, membership,
collapse/orientation, global visible order and retained controls. Synthetic
chrome events are not physical OS drag/trackpad evidence.

Four real loopback pages are seeded with unique document nonces, unsaved form
text, JavaScript counters and navigation state. The native gate must preserve
all of these through six group moves and the split-to-group drop, with exactly
four server document loads throughout. The existing fixed-command actor is
registered only under explicit verification flags and exact loopback origin;
normal browsing does not register it. Its companion transfer verifier is
explicitly disabled for this structure run.

## First native diagnostic: same-document URL gap

Diagnostic run `34572148095`, source
`a4307b34a700630932b390473f6cc6c5a61e39cf`, failed the initial structural
operation before reaching any drag checks. Four real documents were loaded and
seeded, but row 5's tooltip still showed `/transfer`; the unrelated background
tab addition finally refreshed it to `/transfer?step=1`. The unchanged-row
assertion correctly rejected that late title mutation. This exposed missing
same-document location refresh in Flow, not permission to ignore title changes
or expand the set of affected rows.

The revised fixture requires each seeded native URI and exact rendered tooltip
to reach the seeded URL naturally, with loading complete, before observing
structural changes. It does not force a refresh. Two predicate regressions
reject stale native URLs, stale or incorrect tooltips, busy pages and missing
rows. The product location-listener fix and this stricter fixture still require
a subsequent successful native diagnostic; the failed run proves no drag or
live-document retention milestone.

## Second native diagnostic: keyboard-owner baseline

Run `34572711903`, source `9185c2e877ad1becb61c797c036b273edf4d9a76`,
passed natural seeded-URL settling and the first 11 structural operations, then
failed `collapse-retains-active-page-and-heading` on an unrelated `tabindex`
mutation. The four document loads remained the baseline; no drag operation had
run. That report did not identify the mutated row, so a proposed old row-1
keyboard owner is an inference, not established evidence.

The verifier now uses actual Flow Enter for every hierarchy baseline, including
operations that change selection. It requires native selection, the row's sole
roving entry and exact close-control focus to agree before measuring. Baselines
and any unexpected mutation now record owner/selected row indexes and old/new
attribute values. Affected-row sets and unrelated-mutation rejection remain
unchanged. Two regression tests exercise baseline ownership and fail closed on
missing selection, roving ownership, focus or row. A new native run is still
required; these local tests do not prove real drag behavior.

The targeted top-level location listener is now implemented with teardown and
coalesced per-row updates. Five listener/scheduler tests and a 1,000-row renderer
test cover targeted tooltip refresh and unchanged controls; the selection
fixture now supplies Gecko's progress-listener APIs. All 799 integrated tests
and syntax checks pass after this fix. Every other step of the first native
diagnostic passed; only structure failed. A new native run is still required.

The final combined suite after explicit hierarchy baselines passes all 801
tests and syntax checks. A subsequent native run must still prove the full
30-operation and seven live-document checks; neither earlier failed run does.

Those candidate requirements were subsequently satisfied by full staging
34580758825 and the exact-artifact publication recorded above. Physical M3
input/performance and broader accessibility audits remain separate work.
