# 0.67 candidate validation

Pending full native macOS verification. No physical M3 or trackpad audit is claimed.

First candidate `f8469cfa7cf8cb5df113d1758e89655c6fb1af0c`, run
[34925758194](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34925758194),
passed the initial browser integration, architecture/signature, frame/keyboard
and color gates. Actual 1280px normal/focused address input had exactly 6px top
and bottom padding; no visible toolbar pair overlapped, and the inherited VPN
feature was locked off before initialization.

The candidate is not publishable: new Places suggestions lacked awaited native
frecency readiness, the new gesture fixture used replaceable initial blank-page
history, and the older Flow performance fixture assumed a second default
workspace. Corrections retain real bookmark matches, populated native session
history and off-workspace update assertions. The Flow check now also measures
the actual scrolling wrapper and requires nonzero initial scroll. The next
candidate includes shared sidebar text columns and a frame-colored backdrop
behind clipped page corners, identified from native captures.

Second candidate `f02be4148e3117b49339596f7281d890a4bf0d2e`, run
[34926188147](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34926188147),
passed the corrected native Flow performance test. Product-chrome verification
found a fixture API mistake: the imported frecency export is a class, not the
running component. The verifier now obtains the existing service using pinned
Gecko's own Places test-helper route. Gesture verification established real
Back history, successful next-workspace routing and no momentum skipping, but
later trusted events were unexpectedly uncancelled. The next candidate preserves
gestures during in-window focus transfer and adds detailed cancellation/focus
diagnostics; all strict cancellation and page-history assertions remain.

Third candidate `ce5a5518044d0b304f02019681471fec90b41f3b`, run
[34926578059](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34926578059),
passed the complete native gesture gate. All thirteen horizontal/momentum
events were trusted, cancelable and cancelled. The diagnostic in-window blur
occurred immediately after event two while the same window remained active,
corroborating the corrected focus boundary. Vertical input scrolled exactly
180px without a workspace switch. Hidden-edge, revealed-overlay and webpage
isolation checks passed; all three fixture pages retained two-entry histories.

The product gate measured exactly 40px for all three sidebar text columns and
6px normal/focused address insets. The suggestion panel painted, but the seeded
unvisited bookmark query returned only a native search heuristic. The next
fixture uses a real typed history visit, verifies its bookmark record and
positive native frecency, then uses Gecko's user-facing bookmark restriction.
An exact painted bookmark result and anchored popup remain mandatory.

Fourth candidate `25846e0f51e4b034d4b6c5909991037c50fe6ae2`, run
[34927011500](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34927011500),
stopped at the new explicit readiness assertion: its immediate read-only query
still saw the bookmark-only row (visit count zero), although bookmark suggestions
were enabled. The next fixture follows pinned Gecko's `PlacesTestUtils.addVisits`
readiness pattern: await `history.fetch` visibility, then recalculate, then
require agreement between actual visit rows, cached visit count, bookmark
identity and positive ranking. It also uses the correct `TRANSITIONS.TYPED`
constant. These are fixture corrections, not a relaxation of retrieval or UI
requirements and not evidence of a confirmed browser-provider bug.

Fifth candidate `18cbe7fff5c85427c251a077d144e2d6f8cf220d`, run
[34927400743](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34927400743),
confirmed the typed visit through the native history API, but the bookmark join
then returned zero rows. The missing barrier was initial Places bookmark import,
which can complete after window/session restoration and replace an early test
bookmark. The next candidate uses the same
`PlacesBrowserStartup._placesBrowserInitComplete` wait already used by Fluxion's
native defaults and Library gates, before inserting any fixture data. All
database, exact bookmark-result and geometry requirements remain unchanged.

Sixth candidate `ac6f5d5e24ba4245fc1f7f48fb5c9a632f769ca8`, run
[34927676767](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34927676767),
passed the full product-chrome and gesture gates. At 1280 and 800px, normal and
focused address inputs were 32px high with exact 6px top/bottom insets; all
sidebar labels shared a 40px column. Native bookmark results and suggestion
anchors passed at both widths. Visual review identified a remaining "Firefox
Suggest" heading and redundant inner result-panel frame, removed in the next
candidate using the native group-label switch and a single outer surface.
The popup screenshot driver is also changed to avoid refocusing an open popup;
the next captures must establish whether the full background window is retained.

This candidate remains unpublished: its existing structural gate observed five
initial document requests for four fixture pages, before structural operations.
The extra request's cause was not established. The next gate adds per-tab
navigation/window checkpoints and single-profile verifier ownership, preserving
the exact-four baseline and all no-reload/state-identity assertions.

Seventh candidate `cb83b44db962f5f145123282cc6ecd85214d76c5`, run
[34928236261](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34928236261),
passed the updated product gate: no marketing group labels, no inner border or
shadow, unchanged exact geometry. The structural gate passed all 30 checks with
zero unaffected writes; setup showed one window and exact request progression
0→1→2→3→4. Seven live-state checks retained the four original documents.

Three product screenshots showed the complete browser; the first wide-popup
capture still omitted the background window despite visible/active Gecko state.
The next verifier uses actual macOS Command-L and typed query input, waits for
trusted complete input, and then captures without refocusing. This distinguishes
normal native interaction from programmatic search/capture behavior; the wide
capture anomaly is not declared fixed without inspecting that result.

Eighth candidate `a4a8f5845b52afdd1b2235765289a35d0980cab7`, run
[34928638043](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34928638043),
passed trusted Command-L/complete typed-input and all product geometry checks.
The first wide-popup capture remained incomplete. Review found one remaining
programmatic `gURLBar.focus()` before the keyboard driver. The next verifier
removes it and measures focused geometry only after native keyboard input, so
the first address-bar focus itself follows the user's input path. No product
rendering changes are made on the basis of this unclassified capture symptom.

- Shipped workspace gesture core/controller tests cover axis ownership,
  accumulation, momentum, bounds and privileged-input exclusions.
- Workspace persistence tests cover the single fresh default and preservation
  of existing profiles; Flow runtime tests cover stable heading identity.
- Product policy tests cover default/user branches and the locked service gate.
- Native product-chrome gate measures normal/focused address-field insets and
  pairwise toolbar bounds at 1280 and 800 pixels, with real Places suggestions.
- Native gesture gate uses Gecko's trusted wheel router, observes actual sidebar
  scroll offsets and verifies webpage session history remains unchanged.

All existing release gates remain required. A candidate is not published until
the full macOS run and its captured interface have been reviewed.
