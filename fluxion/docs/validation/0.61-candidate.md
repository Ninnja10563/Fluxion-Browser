# Fluxion 0.61 candidate validation

0.60 is published. This candidate integrates named local Memory exclusion
lists from `feature/memory-exclusion-lists`, including the subsequent checkbox
focus geometry refinement and exact-text native mapping-hash verification.
It retains the tested 0.60 flat-tab structural reconciliation.

[Branch validation](exclusion-lists-candidate.md) records all 726 local tests
and native diagnostic run 34541667093 on exact source `4343df7`.
That run passed list/private cleanup, disabled-startup list pruning, actual
semantic retrieval, corrupted-policy storage retention, explicit purge,
Settings geometry and all broader browser gates. It did not test the later
checkbox refinement or exact-text hash change, make an About request, or
produce a distributable DMG.

Final source must pass the full suite, every mandatory native gate, actual
anonymous About HTTP 200, signature/architecture checks and real packaging.
Publication must use that exact artifact, with independent public checksum
verification. No release claim is made until those gates pass.

A subsequent read-only upstream audit found a missing native cold-candidate
boundary: a new excluded URL could be embedded before the final update-complete
cleanup notification. A later batch failure or interruption could retain the
derived vector. Custom retrieval and enriched-store checks already filter the
URL, but those are not a substitute for preventing native indexing. This
pre-existing path is being fixed before full 0.61 staging/publication, with
new cold-candidate, hash-collision and in-flight-policy-change checks.

The adapter now filters candidates before embedding and writes content-free
sentinels for blocked hashes without changing Gecko's batch counts. Exclusion
saves drain pre-existing native writes before final cleanup. The experimental
native semantic URL-bar provider is deregistered while ordinary Places/search
providers remain; this prevents an alternate unfiltered semantic query path.
Startup and adapter failures close model gates without requesting data deletion.

The native privacy gate now observes actual candidate discovery and actual
embedder inputs for a newly visited excluded page and a safe neighbor. It
requires zero blocked model inputs, a retained sentinel, a real safe model
vector, continued next-batch progress and unchanged Places visits. Original
native methods are restored on completion/failure. Separate VM tests check
the helper's assertions/cleanup; they do not substitute for the native gate.

All 742 local regression tests and syntax checks pass for the integrated
cold-candidate/provider-boundary source. Full native release staging remains
required, including the new cold-visit and provider-registry assertions.

Full staging [34543580649](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34543580649)
on `e11f066f27a1a83fc336e46ab748403400a4f6d6` failed before the
cold-candidate check: both the isolation helper and its verification assumed
legacy short provider names. Firefox 155 derives these names from the class,
including `UrlbarProviderSemanticHistorySearch` and `UrlbarProviderPlaces`.
The correction recognizes both known semantic names, retains ordinary Places,
and records each registry condition separately before asserting. Regression
fixtures now reproduce the actual inherited constructor-name getter.
The other native gates passed, including corrupted-policy restart retention.
About returned anonymous HTTP 403 with reset `2026-09-11 00:15:44 UTC`;
no release-discovery retry is allowed before that time. No DMG was packaged
or published. Diagnostics may run without calling the release endpoint.
The corrected source passes all 743 local regression tests and syntax checks.

Native diagnostics [34544567530](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34544567530)
passed every step on exact source `e492f027372277bcf1b14b1f6fc45772994b75d3`.
Both native registries retained ordinary Places and lacked either semantic
provider alias. The cold-candidate check observed zero excluded model inputs,
an actual safe model input/vector, the blocked sentinel, next-batch progress
and retained ordinary history. Named-list cleanup, stale/private write rejection,
disabled startup, corrupt-policy retention and all broader native gates passed.
This run did not contact About or produce a publishable DMG.

The subsequent evidence-only refinement preserves a second actual OS screenshot
of the open seeded exclusion-list editor at a 320-pixel Settings width. It keeps
the original Workspaces capture, requires independent completion receipts,
and restores the fixture. All 745 local checks pass; native capture verification
and full staging are still required for this later source.

The second native run produced both OS screenshots. Inspection of the actual
narrow editor confirmed visible field focus and contained controls, and found
a singular-count copy defect. The summary now says `1 domain` while retaining
`0 domains` and plural counts; a live Settings test verifies updates preserve
the disclosure node. All 746 local checks pass for the final candidate.

The feature groups domains supplied by the user; it is not an automatic site
category classifier. Policy edits logically delete matching enriched evidence
and scrub native vectors while retaining Places history, not forensically
erase storage media. Old previews do not understand canonical list policy;
Memory/page AI must be disabled before downgrading a migrated profile.
