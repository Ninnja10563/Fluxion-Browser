# Fluxion 0.62 candidate validation

Published as [0.62.0-preview.1](https://github.com/Ninnja10563/Fluxion-Browser/releases/tag/v0.62.0-preview.1).
Final full staging [34570463330](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34570463330)
on `b82978d1603becd68a98bf395bdd972c9003ee60` passed all 770 tests, every native
gate, actual anonymous About HTTP 200, universal architecture/signature checks
and real DMG packaging. The strengthened global ordering assertion ran across
all hierarchical operations in the real 1,000-tab gate. The exact staged DMG
was promoted without rebuilding. See [provenance](../../release/provenance/v0.62.0-preview.1.md).

The chronological candidate record below predates that successful final staging.
This milestone extends Flow's keyed updates to groups and splits instead of
rebuilding every sidebar row whenever those structures change.

Tab and control identity survives group-level changes and split wrapping.
Group headings retain unique accessible child-container IDs. Split wrappers
are projection-local; collapsed groups continue showing only the active page.
The whole tree plan is validated, new parents are connected first, and obsolete
containers are pruned only after surviving descendants are reparented. Focus
recovery respects external controls, and pointer-close holding still defers
structural work. Native object identity prevents restored objects with reused
IDs from inheriting stale event handlers.

All 763 local tests and syntax checks passed before the final native-verifier
extension. That extension uses Firefox 155's documented native
`group.addTabs([splitWrapper])` path and real split reversal, not fabricated
group/split objects. Final integrated local and macOS diagnostics remain required.

The final integrated candidate, including that native verifier and the updated
health-marker contract, also passes all 763 local tests and syntax checks.

Native diagnostics [34570068097](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34570068097)
passed every step on `a2a44dcc92faba349252af391e08d4cb50d5bea0`.
The real 1,000-tab structure gate passed 21 operations, with zero writes to
unaffected rows and one relocation for an adjacent swap. Native grouped splits,
collapse/expansion, reversal, separation, heading IDs and control identity were
verified. All broader browser, privacy and restart checks also passed.
This diagnostic did not make an About request or produce a publishable DMG.

A subsequent verifier review added an overall visible-order assertion, separate
from each group's child-order checks, so an unrelated root row cannot move or
duplicate undetected. Seven tests exercise the shipped assertion's rejection
paths. All 770 integrated local tests pass; final full staging must exercise
the stronger assertion in real Gecko before publication.

Successful diagnostics alone cannot publish a DMG: full release staging must
also pass all browser gates, the actual anonymous About request, signature and
architecture checks, real packaging, and exact-artifact/public-checksum
verification. No physical-hardware frame-rate claim is made.
