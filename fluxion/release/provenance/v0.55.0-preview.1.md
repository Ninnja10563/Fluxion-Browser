# Fluxion 0.55.0-preview.1 build provenance

- Built source: `c1363fdb5adf0ff88c7874c59e4b7073bdcbe44d`.
- Successful staging: [34517093293](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34517093293), attempt 1.
- Workflow: `.github/workflows/macos-preview-release.yml`, dispatched on `main`
  in `Ninnja10563/Fluxion-Browser` with version `0.55.0-preview.1`.
- All 582 regression tests passed without failures or skips. Every required
  native gate, packaging and artifact upload succeeded. Automatic publication
  alone was skipped because this was explicit staging.
- Required new native evidence: exact-PID macOS System Events tab/group menu
  navigation and Escape focus restoration; 1,000-tab bounded search with live
  workspace renaming and retained input focus; stacked-picker activation;
  cross-window General/Appearance controls and focused homepage-draft protection.
- Existing browsing, authentication, actual macOS file-picker upload, Library,
  live tab adoption, clean/SIGKILL recovery, privacy, semantic Memory, native
  external opening, accessibility and AI boundary checks also passed.
- Real anonymous update discovery returned HTTP 200, with one GET, no
  credentials, cookies or referrer, and no asset download.
- Exact artifact: `10168319424`, `Fluxion-0.55.0-preview.1-macOS-universal`.
- GitHub-reported archive SHA-256:
  `7d133adcded9ccf07deab5a11e524a641a12286d2b209e9184eafd9ce6351171`.
- DMG: `Fluxion-0.55.0-preview.1-macOS-universal.dmg`.
- DMG size: `224761804` bytes.
- Verified DMG SHA-256:
  `bf0e25e9482a53f3e12aeb87c536afaab02bdb8e49365e9446799bc9088449e5`.

The complete staging download contained exactly the regular DMG and its
111-byte checksum file. The first local extraction ran out of temporary-disk
space; that incomplete copy was not published. A new complete download on a
larger disk was hashed successfully before publication. No macOS workflow was
rerun or bypassed because of this local download issue.

The successful run's dark Settings/Flow, light Library and About screenshots
were inspected, and an independent gate/artifact audit passed. The
[public prerelease](https://github.com/Ninnja10563/Fluxion-Browser/releases/tag/v0.55.0-preview.1)
promotes those unchanged tested bytes, not a rebuild. Both asset states are
uploaded: `555630248` (DMG) and `555630245` (checksum). The remote Git tag resolves
to the built SHA above, not a later documentation commit.

The public DMG was streamed back without authentication and independently
hashed. Its 224,761,804 bytes and SHA-256 match the complete staging download
and the independently fetched public checksum file.

See [candidate validation](../../docs/validation/0.55-candidate.md) for earlier
diagnostic runs and explicit distinctions between DOM-driven Settings tests,
native OS-keyboard menu tests, and hosted-runner timing. No high-refresh-rate,
exhaustive website compatibility, or full-roadmap completion claim is made.

This is a universal Apple Silicon/Intel development prerelease, ad-hoc signed
and not Developer-ID-signed or Apple-notarized.
