# Fluxion 0.57.0-preview.1 build provenance

- Built source: `826c712f08d060852b119a78a70e9219851e7d62`.
- Successful staging: [34523416371](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34523416371), attempt 1.
- Workflow: `.github/workflows/macos-preview-release.yml`, dispatched on `main`
  in `Ninnja10563/Fluxion-Browser` with version `0.57.0-preview.1`.
- All 601 regression tests passed without failures or skips. Every required
  native gate, packaging and artifact upload succeeded. Automatic publication
  alone was skipped because this was explicit staging.
- New native evidence: all ten Settings sections at actual 320 and 600 pixel
  root widths, 184 rendered controls, no horizontal page overflow, visible
  permanent/session/timed permission expiry, genuine workspace form creation
  and field renaming, and origin/context-specific native accessible reset names.
- Existing browsing, authentication, actual macOS file-picker upload, Library,
  live tab adoption, clean/SIGKILL recovery, privacy, semantic Memory, external
  opening, sidebar sizing, shortcuts, focus and AI boundary checks also passed.
- Real anonymous update discovery returned HTTP 200, with one GET, no
  credentials, cookies or referrer, and no asset download.
- Exact artifact: `10170795591`, `Fluxion-0.57.0-preview.1-macOS-universal`.
- GitHub-reported archive SHA-256:
  `ff4877cb29e91c76af3d4234ac27678eefb18694ad5293929e89434e120a74c8`.
- DMG: `Fluxion-0.57.0-preview.1-macOS-universal.dmg`.
- DMG size: `224774639` bytes.
- Verified DMG SHA-256:
  `0e6bcbe27d20176b9cb2cf4b59181dea2039edb2f3f74445591d9fd596aaca93`.

The complete staging download contained exactly the regular DMG and its
111-byte checksum file. Its checksum passed before publication. Successful
staging screenshots of narrow Workspaces, dark Settings/Flow, light Library
and About were inspected. Independent layout and release-gate audits passed.

The [public prerelease](https://github.com/Ninnja10563/Fluxion-Browser/releases/tag/v0.57.0-preview.1)
promotes those unchanged tested bytes, not a rebuild. Both assets are uploaded:
`555732217` (DMG) and `555732219` (checksum). The remote Git tag resolves to the
built source above, not this later documentation commit. The separately fetched
anonymous public checksum matches the staged checksum.

The public DMG was streamed back without authentication and independently
hashed: all 224,774,639 bytes and its SHA-256 match the staged DMG and the
separately fetched public checksum. Publication did not alter the tested bytes.

See [candidate validation](../../docs/validation/0.57-candidate.md) for the
initial name-length fixture correction, passing diagnostics and evidence
boundaries. These Settings layout checks operate through the actual privileged
DOM and Gecko accessibility service, not physical OS input or an exhaustive
screen-reader audit. Long editable names retain native single-line input
scrolling; no page-level horizontal overflow occurred at the tested widths.
No high-refresh-rate or full-roadmap completion claim is made.

This is a universal Apple Silicon/Intel development prerelease, ad-hoc signed
and not Developer-ID-signed or Apple-notarized.
