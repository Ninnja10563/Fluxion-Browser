# Fluxion 0.58.0-preview.1 build provenance

- Built source: `16dfb49d0fd8bc71e47e53cb13f8040179327366`.
- Successful staging: [34531228585](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34531228585), attempt 1.
- Workflow: `.github/workflows/macos-preview-release.yml`, dispatched on `main`
  in `Ninnja10563/Fluxion-Browser` with version `0.58.0-preview.1`.
- All 631 tests passed with no failures or skips. Every required native check,
  DMG/checksum packaging and artifact upload succeeded. Only automatic
  publication was skipped because this was explicit staging.
- Native selection: 1,000 Gecko tabs, 30 ordinary selections, retained row and
  close-control identity, zero unrelated row writes, zero no-op attributes and
  no list rebuilding. Native multi-selection, routed clicks, DOM Enter,
  pinned/tree keyboard entries, group projections and split state passed.
- Hosted event-to-frame timing: p50 18.30 ms, p95 34.77 ms, maximum 42.66 ms.
  This used 40 eager and 960 lazy fixture browsers in a non-OS-active window;
  it is not physical input latency or a high-refresh-rate guarantee.
- Separate native tab/group menu verification used actual owned-process
  System Events keys, trusted leaf-highlight acknowledgements, native submenu
  opening and Escape focus restoration. Header event-loop settling was not
  treated as proof of processed input.
- Browsing, authentication, real macOS file-picker upload, Library, transfers,
  workspace editing, clean/SIGKILL recovery, private-data exclusion, semantic
  Memory, responsive Settings, external opening and AI boundaries also passed.
- Actual anonymous update discovery returned HTTP 200: one GET, no credentials,
  cookies or referrer, and no asset download.
- Unique unexpired artifact: `10173783780`, `Fluxion-0.58.0-preview.1-macOS-universal`.
- GitHub-reported archive SHA-256:
  `2bcff5b66848314d5ae812159048f0cabbe37713dc29b4cf38fbbae732cb02bd`.
- DMG: `Fluxion-0.58.0-preview.1-macOS-universal.dmg`.
- DMG size: `224783196` bytes.
- Verified DMG SHA-256:
  `e4d16c732645f0a61542e8a583a418fbd0400b985e98cef864429562e31aa8d0`.

The staging download contained exactly the regular DMG and its 111-byte
checksum file. The checksum passed before upload. Successful staging captures
of dark Settings/Flow, narrow Workspaces, light Library and About were inspected.
An independent audit verified source, workflow, all gates and artifact identity.

The [public prerelease](https://github.com/Ninnja10563/Fluxion-Browser/releases/tag/v0.58.0-preview.1)
promotes those unchanged tested bytes, not a rebuild. Both assets are uploaded:
`555850189` (DMG) and `555850190` (checksum). The tag targets the exact built
source above, not this later documentation commit. The independently fetched
anonymous public checksum matches the staged checksum.

The public DMG was streamed back without authentication and independently
hashed: all 224,783,196 bytes and its SHA-256 match the staged DMG and the
separately fetched public checksum. The remote tag was independently checked
against the built source. Publication did not alter the tested bytes.

See [candidate validation](../../docs/validation/0.58-candidate.md) for prior
failures, the native event-target correction, precise keyboard-entry fixture
contract, external GitHub quota wait and fixture ownership sequencing. No
failed artifact was promoted and no mandatory release check was bypassed.
The 6,010-to-4,006 session-read reduction is measured by instrumented shipped
functions, not a claim of proportional hardware speedup.

This is a universal Apple Silicon/Intel development prerelease, ad-hoc signed
and not Developer-ID-signed or Apple-notarized. The full roadmap is not complete.
