# Fluxion 0.59.0-preview.1 build provenance

- Built source: `22b980ec31cac685dcd94078587c61df2b595320`.
- Successful staging: [34537234654](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34537234654), attempt 1.
- Workflow: `.github/workflows/macos-preview-release.yml`, dispatched on `main`
  in `Ninnja10563/Fluxion-Browser` with version `0.59.0-preview.1`.
- All 668 tests passed with no failures or skips. Every required native check,
  DMG/checksum packaging and artifact upload succeeded. Only automatic
  publication was skipped because this was explicit staging.
- Native privacy verification scrubbed an encoded-sensitive vector while
  retaining its Places visit and a safe neighboring vector. Old enriched
  sensitive text/vector counts became 0/0, with safe counts 1/1.
- An independent real relaunch, with Memory disabled, observed startup removal
  of sensitive enriched text/vectors through read-only SQLite, without invoking
  cleanup from the verifier. Safe evidence/vector bytes and Places visits
  remained. Explicit synthetic vectors test storage behavior, not inference;
  the separate real local semantic retrieval gate also passed.
- Broad real browsing, native sanitizer cancellation, Library OS menu input,
  native upload, 1,000-tab selection, workspaces, cross-window tab transfer,
  clean/SIGKILL recovery, private isolation, external opening, responsive
  Settings and AI credential/sharing boundaries all passed.
- Actual anonymous update discovery returned HTTP 200: one GET, no credentials,
  cookies or referrer, and no asset download.
- Unique unexpired artifact: `10176042223`, `Fluxion-0.59.0-preview.1-macOS-universal`.
- Downloaded archive SHA-256 matched GitHub's digest:
  `410810cdd25322eba6170dcbb1fcc2789863e94557938058857232efcf82678c`.
- DMG: `Fluxion-0.59.0-preview.1-macOS-universal.dmg`.
- DMG size: `224790303` bytes.
- Verified DMG SHA-256:
  `42352a96012249f161bfed3e17742cf876b22e1248e989e4b719716d28fe53ee`.

The downloaded ZIP contained exactly the regular DMG and its 111-byte checksum
file. The checksum passed before upload. Actual dark Search & Memory/Flow,
light Library, narrow Workspaces and About captures were inspected. An
independent audit verified the source, every gate and artifact identity.

The [public prerelease](https://github.com/Ninnja10563/Fluxion-Browser/releases/tag/v0.59.0-preview.1)
promotes those unchanged tested bytes, not a rebuild. Both assets are uploaded:
`555947523` (DMG) and `555947522` (checksum). The remote tag targets the exact
built source above, not this later documentation commit.

An independent anonymous public download returned HTTP 200 and streamed all
224,790,303 DMG bytes through SHA-256. Both the size and digest matched the
staged DMG and the separately fetched 111-byte public checksum. No credentials,
cookies or referrer were sent. Public asset metadata and the remote tag were
also independently checked; publication did not change the tested bytes.

See [candidate validation](../../docs/validation/0.59-candidate.md) for the
earlier failed staging runs, quota cooldown, native input handshake and
sanitizer readiness correction. No failed artifact was promoted and no
mandatory check was bypassed. Sensitive-route detection remains heuristic;
logical deletion does not guarantee forensic erasure from media or backups.

This is a universal Apple Silicon/Intel development prerelease, ad-hoc signed
and not Developer-ID-signed or Apple-notarized. The full roadmap is not complete.
The separate flat-tab structural reconciliation branch and its later manual
packaging-version safeguards are not included in this release.
