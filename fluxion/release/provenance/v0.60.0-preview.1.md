# Fluxion 0.60.0-preview.1 build provenance

- Built source: `b686aa51bfb31b4008613c0655c390acfa690b2a`.
- Successful staging: [34541225358](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34541225358), attempt 1.
- Workflow: `.github/workflows/macos-preview-release.yml`, dispatched on `main`
  in `Ninnja10563/Fluxion-Browser` with version `0.60.0-preview.1`.
- All 687 tests passed with no failures or skips. Every required native gate,
  signature/architecture check, real DMG packaging and artifact upload passed.
  Only automatic publication was skipped because this was explicit staging.
- The structural gate passed all seven checks with 1,000 native Gecko tabs:
  unaffected-row writes stayed at zero, the adjacent swap used one DOM
  relocation, and all six operation baselines retained selected-tab ownership,
  exact control focus and the intended keyboard entry. Group/split fallbacks
  and guarded cleanup passed.
- Real browsing, transfers/authentication, native upload dialogs, private
  boundaries, actual local semantic retrieval, native/enriched Memory cleanup,
  clean/SIGKILL recovery, external opening, responsive Settings, and AI
  credential/sharing revocation passed.
- About discovery made one actual anonymous GET with HTTP 200, without
  credentials, cookies, referrer or automatic asset downloads.
- Unique unexpired artifact: `10177486518`, `Fluxion-0.60.0-preview.1-macOS-universal`.
- Downloaded ZIP size: `222602531` bytes; its SHA-256 matched GitHub's digest:
  `fa04fe4f5a9ca96554d58ce6fa0fa515eacc6b87338ce2be7c6ad08a7f5f983d`.
- ZIP inspection found exactly two regular files: the DMG and its checksum.
- DMG: `Fluxion-0.60.0-preview.1-macOS-universal.dmg`, `224683818` bytes.
- Verified DMG SHA-256:
  `39d1eb1d1d6cae6f82f12e688df388a19e247593d670bc4ad0a420affa36bbff`.
- The 111-byte checksum file has SHA-256:
  `8c3d51852c792ce090683ca600cb819556404d7f10c6cd56b7e81d093949478b`.

Actual dark Search & Memory/Flow, light Library, narrow Workspaces and About
captures were inspected. The packaged manual-version safeguards were exercised
by real macOS packaging, not only the six local stubbed-tool shell tests.

The [public prerelease](https://github.com/Ninnja10563/Fluxion-Browser/releases/tag/v0.60.0-preview.1)
is release `386686729`, with DMG asset `556015749` and checksum `556015751`.
Both uploaded asset digests matched before the draft was published. An
independent anonymous public download returned HTTP 200 and streamed all DMG
bytes through SHA-256, matching the staged file and separately fetched public
checksum. The remote tag resolves to the exact built source above, not this
later documentation commit. No rebuild or changed artifact was promoted.

Earlier failed staging and the respected About rate-limit cooldown are recorded
in [candidate validation](../../docs/validation/0.60-candidate.md). No mandatory
gate was bypassed. Mutation/focus checks do not establish physical input latency
or sustained high-refresh-rate performance; complex topology still uses the
full-render fallback. Named Memory exclusion lists remain a separate candidate.

This universal Apple Silicon/Intel development prerelease is ad-hoc signed,
not Developer-ID-signed or Apple-notarized. The full roadmap remains unfinished.
