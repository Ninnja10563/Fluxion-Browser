# Fluxion 0.54.0-preview.1 build provenance

- Built source: `d14e330d489eee2f9d31056671041448a045316a`.
- Successful staging: [34516576202](https://github.com/Ninnja10563/Fluxion-Browser/actions/runs/34516576202), attempt 1.
- Workflow: `.github/workflows/macos-preview-release.yml`, dispatched on the
  preserved `release/0.54-preview` branch in `Ninnja10563/Fluxion-Browser`.
- All 561 regression tests passed with no failures or skips. Every required
  native gate, packaging and artifact upload succeeded. Automatic publication
  alone was skipped because this was explicit staging.
- Native checks include live cross-window document adoption, retained hidden
  workspace pages, groups/pins/containers/splits, normal/private separation,
  adopted-tab clean and SIGKILL recovery, actual file-picker uploads, ordinary
  browsing and authentication, Library, Memory and AI privacy boundaries.
- Actual command-palette activation retained the stacked split orientation.
  The full native clear-data dialog surface and cancellation checks passed.
- Real anonymous update discovery returned HTTP 200, using one GET without
  credentials, cookies or referrer, and without downloading assets.
- Exact artifact: `10168100571`, `Fluxion-0.54.0-preview.1-macOS-universal`.
- GitHub-reported archive SHA-256:
  `74206c36f52fb5842e531f2c65b95ed5002e2353a7b2f7b65b9b05e99cb66bb0`.
- DMG: `Fluxion-0.54.0-preview.1-macOS-universal.dmg`.
- DMG size: `224594234` bytes.
- Verified DMG SHA-256:
  `6226d6ffad8d2b91a03de208be38630043417354bfd4f677f0ecf7bafa7096b6`.

The exact staging download contained only the regular DMG and its 111-byte
checksum file. Local hashing matched that checksum before publication.
The same successful run's dark Settings/Flow, light Library and About
screenshots were inspected. An independent gate/artifact audit also passed.

The [public prerelease](https://github.com/Ninnja10563/Fluxion-Browser/releases/tag/v0.54.0-preview.1)
promotes those unchanged bytes, not a rebuild. Asset IDs are `555617368` (DMG)
and `555617367` (checksum), both uploaded. The remote Git tag resolves to the
built SHA, not this subsequent documentation commit. The public DMG was streamed
back without authentication and independently hashed: its exact byte count
and SHA-256 match the staged DMG and public checksum.

Earlier failures remain recorded in [candidate validation](../../docs/validation/0.54-candidate.md):
`34512470993` failed anonymous update quota, and `34515354934` did not finish
the clear-data dialog verification chain. No failed artifacts were published
and no required gate was bypassed. Dialog initialization readiness and exception
reporting were corrected before this successful staging run.

This universal Apple Silicon/Intel app is an ad-hoc-signed development preview,
not Developer-ID-signed or Apple-notarized. The original roadmap is not complete.
